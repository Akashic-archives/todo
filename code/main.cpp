#include <iostream>
#include "object.h"

int main() {
	std::cout << "Hello World!" << std::endl;

	//ls, done/done(all), remove/remove(all), make, change, update(time), priority, rank, sous-todo ex math exam devoir, count, head, tail, localisation

	return 0;
}
